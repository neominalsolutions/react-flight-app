let a: number = 10;

export function Sum(x: number, y: number) {
	return x + y;
}

interface Person {
	name: string;
}

class User implements Person {
	constructor() {}

	name: string = '';
}

// return <div></div>
